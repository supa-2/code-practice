def generate_skipgram_pairs(sentence, window):
    """
    生成 Skip-Gram (中心词, 上下文词) 样本对

    参数:
        sentence: list[str], 已分词的句子
        window: int, 上下文窗口大小（左右各 window 个）
    返回:
        list[tuple[str, str]]
    """
    # TODO: 你的代码（约 8-12 行）
    pass
