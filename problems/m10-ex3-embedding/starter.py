import numpy as np

def lookup(embedding, vocab, word):
    """从 embedding 矩阵查出 word 的向量"""
    # TODO: 约 2 行
    pass


def most_similar(embedding, vocab, target_vec, exclude=None):
    """找到与 target_vec 余弦相似度最高的词（排除 exclude 列表）"""
    if exclude is None:
        exclude = []
    # TODO: 约 8-12 行
    pass
